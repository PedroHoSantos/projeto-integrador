export default {
    darkMode: "class",
    content: ["./index.html", "./src/**/*.{js,jsx}"],
    theme: {
      extend: {
        colors: {
          primary: "#37517E",   // Azul petróleo (principal)
          highlight: "#6FA9FF", // Azul claro (links e destaques)
          bgLight: "#F7F7F7",   // Fundo claro suave
          textBase: "#2B2B2B",  // Texto principal
        },
      },
    },
    plugins: [],
  };
  